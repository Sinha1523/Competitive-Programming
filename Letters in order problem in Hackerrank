#include <bits/stdc++.h>
using namespace std;

int main() {
    string S;
    cin >> S; 
    set<char> unique; 
    for (char c : S) {
        unique.insert(c);
    }
    for (char c : unique) {
        cout << c;
    }
    cout << endl; 
    return 0;
}
